手机首页竖屏背景

把图片放在这个文件夹，按顺序命名：
001.png
002.png
003.png

也支持 .jpg、.jpeg、.webp，扩展名用小写。请真正导出为对应格式，不要只修改扩展名。
从 001 开始连续编号，不要跳号；每个编号只放一张。最多 99 张。
图片高度必须大于宽度，建议使用 1080 × 1920。
放入后刷新网页，即可点按首页背景切换。无需修改代码。
暂未放入图片时，手机首页显示渐变背景，不再使用电脑端旧图。
电脑端图片仍保存在 images/hero，互不影响。
高级设置在项目根目录 mobile-backgrounds.js。
